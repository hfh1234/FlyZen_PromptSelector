自制提示词选择器，可以在提示词框中按指定格式随意输入任意任意数量的提示词，并手动选择需要输出的提示词

This custom-designed prompt selector allows you to easily input any number of prompts in a specified format into the prompt box and manually select the prompts to be output.
